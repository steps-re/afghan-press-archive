"""htr-toolkit: a script-agnostic, layout-aware, multi-vendor HTR platform.

Layers: pages (I/O) -> layout (structure) -> models (reader bank) -> metrics (eval spine).
Everything runs on free credits (Gemini/the credit grant + GPT-5.x/Azure-MS)."""
